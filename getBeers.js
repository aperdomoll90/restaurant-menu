const coffees = require('./data/allBeer.json')

exports.getBeers =  (name) => {
    beers.forEach = (beerRecipe) => {
        if(beerRecipe.ratings.reviews >= 300 ) {
          console.log(name,'loves', beerRecipe.name, 'review',beerReview)
        }
    }
    }